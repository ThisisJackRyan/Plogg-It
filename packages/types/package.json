{
  "name": "@plogg/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TS types and Zod schemas for Plogg Club (pins, profiles, enums).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./database": "./src/database.generated.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@plogg/config": "*",
    "typescript": "^5.7.2"
  }
}
